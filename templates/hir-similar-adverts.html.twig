<div class="panel panel-primary similar-adverts-block">
    <div class="panel-heading text-center">{{ title }}</div>
    <ul class="list-group">
        {% for advert in adverts %}
            <li class="list-group-item">
                <div class="media">
                    <div class="media-left">
                        <a href="{{ path('entity.node.canonical', {'node':advert.id}) }}">
                            <img src="{{ advert.field_advert_picture[0].entity.uri.value|image_style('smallest_50x50') }}" class="media-object img-rounded" alt="{{ advert.title.value }}">
                        </a>
                    </div>
                    <div class="media-body">
                        <h4 class="media-heading">{{ advert.title.value|striptags|slice(0, 20)|raw }} <small><i>Ref: {{ advert.field_advert_reference.value }}</i></small></h4>
                        <p>Excellent feature! I love it. One day I'm definitely going to put this Bootstrap component into use and I'll let you know once I do.</p>
                    </div>
                </div>
            </li>
        {% endfor %}
    </ul>
</div>