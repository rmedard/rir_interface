<div class="panel panel-success">
    <div class="panel-heading"><span class="glyphicon glyphicon-signal"></span> {{ title }}</div>
    <ul class="list-group">
        <li class="list-group-item"><span class="badge">{{ rent }}</span>Rent adverts </li>
        <li class="list-group-item"><span class="badge">{{ sale }}</span>Sale adverts </li>
    </ul>
</div>