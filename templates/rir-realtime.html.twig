<div class="panel panel-success">
    <div class="panel-heading"><span class="glyphicon glyphicon-stats"></span> {{ title }}</div>
    <ul class="list-group">
        <li class="list-group-item"><span class="badge">{{ rent }}</span>Rent adverts since 2017</li>
        <li class="list-group-item"><span class="badge">{{ sale }}</span>Sale adverts since 2017</li>
    </ul>
</div>