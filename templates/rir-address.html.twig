<div class="card border-secondary">
    <div class="card-header bg-secondary text-white"><i class="fas fa-home"></i> {{ title }}</div>
    <div class="card-body">
        <p class="m-0 fw-bold">{{ drupal_token('site:name') }} - HiR Ltd</p>
        <p class="m-0"><i class="fas fa-map-marker-alt"></i> KG 611 St #10 Gishushu</p>
        <p class="m-0"><i class="fas fa-envelope"></i> P.O. Box 4062 Kigali, Rwanda</p>
        <p class="m-0"><i class="fas fa-phone"></i> {{ drupal_token('custom_token:phone') }}</p>
        <p class="m-0"><i class="fas fa-at"></i> {{ drupal_token('site:mail') }}</p>
    </div>
</div>
